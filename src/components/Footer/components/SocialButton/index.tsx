import React from 'react';
import { Button } from '@chakra-ui/react';

const SocialButton = () => {
    return (
        <Button>
            SocialButton
        </Button>
    )
}

export { SocialButton }